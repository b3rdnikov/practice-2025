name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: mv site/* ./  # Переносит файлы из /site в корень при деплое
      - uses: actions/deploy-pages@v3
